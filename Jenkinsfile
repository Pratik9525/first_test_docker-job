pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo "Build the job"
            }
        }
        stage('test') {
            steps {
                echo "test the job"
            }
        }
        stage('deploy') {
            steps {
                echo "deploy the job"
            }
        }
        
    }
}
